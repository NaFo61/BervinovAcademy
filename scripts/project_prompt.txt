================================================================================
КОНТЕКСТ РЕПОЗИТОРИЯ (автогенерация для LLM)
Bervinov Academy
================================================================================

## ДЛЯ МОДЕЛИ (как использовать этот файл)

  • Назначение: одноразовый снимок структуры и ключевых точек входа; не замена чтению кода.
  • Источник истины: файлы в репозитории; при конфликте доверять коду, не этому тексту.
  • Языки: интерфейс и комментарии часто на русском; имена в коде и API — латиница.
  • Репозиторий: монолит — `backend/` (Django), `frontend/` (React + Vite), `nginx/` (прокси в Docker).
  • Обновление: перегенерировать командой `python scripts/generate-prompt.py`.

## ОБЗОР ПРОЕКТА

  Название: Bervinov Academy
  Версия (pyproject): 0.1.0
  Описание: образовательная платформа (курсы, уроки, менторство, прогресс, подписки); бэкенд REST на Django.
  Тип: fullstack — Django REST + SPA на React.
  Сгенерировано (UTC): 2026-06-17T16:38:17Z

## КАК СВЯЗАНЫ ЧАСТИ (поток запросов)

  Разработка (локально):
    1) Backend: `python manage.py runserver` из каталога `backend/` (порт 8000 по умолчанию).
    2) Frontend: `npm run dev` в `frontend/` — Vite на порту 3000; в `vite.config` прокси `/api` → backend.

  Production (Docker / nginx):
    Браузер → nginx (порт 80) → статика SPA из `/`; пути `/api/`, `/admin/`, `/static/`, `/media/` проксируются на контейнер backend.

  Схема (упрощённо):
    [клиент] --HTTP--> [nginx или Vite dev] --/api--> [Django DRF]

## ТОЧКИ ВХОДА BACKEND

  • Корневые URL: `backend/school_platform/urls.py` — монтирует `api/` → приложения users, content, progress;
    также admin, health, drf-spectacular (swagger/redoc).
  • Настройки: `backend/school_platform/settings.py`.
  • ASGI/WSGI: `asgi.py`, `wsgi.py`; Celery: `celery.py`.

## FRONTEND (маршруты SPA)

  Файловый роутинг TanStack Router; сгенерированное дерево: `frontend/src/routeTree.gen.ts`.
  Пути страниц:
  (frontend/src/routeTree.gen.ts не найден — соберите роутер TanStack)

  Скрипты package.json:
  npm run build
  npm run dev
  npm run preview

## СТЕК (кратко)

  Backend: Python 3.11+, Django, DRF, JWT (simplejwt), Celery, Redis, PostgreSQL.
  Frontend: React 18, TypeScript, Vite, TanStack Router, Tailwind v4 (@tailwindcss/vite), axios.

  Фрагмент requirements.txt:
  | django==4.2
  | python-decouple==3.8
  | psycopg2-binary==2.9.10
  | pillow==12.0.0
  | django-unfold==0.68.0
  | celery==5.5.3
  | redis==7.0.1
  | deep-translator>=1.11.0
  | django-modeltranslation
  | Faker==38.2.0
  | Unidecode==1.4.0
  | djangorestframework==3.16.1
  | djangorestframework-simplejwt==5.5.1
  | pre-commit>=3.0.0
  | black>=23.0.0
  | isort>=5.12.0
  | mypy>=1.0.0
  | pytest==8.3.5
  | pytest-django==4.9.0
  | pytest-cov==6.0.0
  | pytest-xdist==3.6.1
  | model-bakery==1.20.0
  | drf-spectacular[sidecar]>=0.27.0
  | rpds-py>=0.8.0
  | django-cors-headers==4.3.1
  | kafka-python==2.0.2
  | livekit-api>=1.0.0

## DJANGO-ПРИЛОЖЕНИЯ (папки в backend/ с apps.py)

  • communication: models, views, viewsets, serializers, urls
  • content: models, viewsets, serializers, urls
  • education: models, views, viewsets, serializers, urls
  • exams: models, views, serializers, urls
  • fixture: конфиг/утилиты
  • mentoring: models, views, urls
  • progress: models, views, viewsets, serializers, urls
  • subscriptions: models, views
  • translations: models
  • users: models, viewsets, serializers, urls

## ДЕРЕВО РЕПОЗИТОРИЯ (укороченное)

  backend/
    common/
      [py] __init__.py
      [py] drf.py
      [py] models.py
    communication/
      migrations/
        [py] 0001_initial.py
        [py] __init__.py
      tests/
        [py] __init__.py
        [py] test_conference_api.py
      [py] __init__.py
      [py] admin.py
      [py] apps.py
      [py] livekit_tokens.py
      [py] models.py
      [py] serializers.py
      [py] services.py
      [py] tasks.py
      [py] tests.py
      [py] urls.py
      [py] views.py
      [py] viewsets.py
    content/
      migrations/
        [py] 0001_initial.py
        [py] 0002_alter_course_options_alter_lessontheory_options_and_more.py
        [py] 0003_lessonradioquestion_lessoncheckboxquestion_and_more.py
        [py] 0004_alter_answeroption_options_and_more.py
        [py] 0005_rename_answeroption_radioansweroption.py
        [py] 0006_codingchallenge_testcase_and_more.py
        [py] 0007_alter_codingchallenge_unique_together_and_more.py
        [py] 0008_codingchallenge_slug.py
        [py] 0009_uuid_public_ids.py
        [py] 0010_remove_codingchallenge_slug.py
        [py] 0011_module_lesson_shared_order.py
        [py] 0012_lesson_video_fields.py
        [py] 0013_exam_system.py
        [py] __init__.py
      tests/
        [py] __init__.py
        [py] conftest.py
        [py] test_exam_lesson_api.py
        [py] test_models.py
        [py] test_video_utils.py
      [py] __init__.py
      [py] admin.py
      [py] apps.py
      [py] container_lessons.py
      [py] lesson_parent.py
      [py] lesson_querysets.py
      [py] models.py
      [py] module_lessons.py
      [py] serializers.py
      [py] urls.py
      [py] video_utils.py
      [py] viewsets.py
    education/
      migrations/
        [py] 0001_initial.py
        [py] __init__.py
      tests/
        [py] __init__.py
        [py] test_enrollment.py
      [py] __init__.py
      [py] admin.py
      [py] apps.py
      [py] models.py
      [py] serializers.py
      [py] services.py
      [py] tests.py
      [py] urls.py
      [py] views.py
      [py] viewsets.py
    exams/
      migrations/
        [py] 0001_exam_system.py
        [py] __init__.py
      tests/
        [py] __init__.py
        [py] test_exam_api.py
      [py] __init__.py
      [py] admin.py
      [py] apps.py
      [py] models.py
      [py] serializers.py
      [py] services.py
      [py] urls.py
      [py] views.py
    fixture/
      management/
        commands/
        [py] __init__.py
      [py] __init__.py
      [py] apps.py
    locale/
      en/
        LC_MESSAGES/
      ru/
        LC_MESSAGES/
    mentoring/
      migrations/
        [py] __init__.py
      tests/
        [py] __init__.py
        [py] test_mentor_api.py
      [py] __init__.py
      [py] admin.py
      [py] apps.py
      [py] models.py
      [py] permissions.py
      [py] services.py
      [py] tests.py
      [py] urls.py
      [py] views.py
    progress/
      management/
        commands/
        [py] __init__.py
      migrations/
        [py] 0001_initial.py
        [py] 0002_codesubmission_and_more.py
        [py] 0003_rename_submission_public_id_answer_public_ids.py
        [py] 0004_alter_codesubmission_public_id.py
        [py] 0005_alter_useranswerradio_unique_together.py
        [py] 0006_userlessontheoryread.py
        [py] 0007_achievement_models.py
        [py] 0008_seed_achievements.py
        [py] 0009_exam_system.py
        [py] __init__.py
      tests/
        [py] __init__.py
        [py] conftest.py
        [py] test_kafka_results.py
        [py] test_profile_stats.py
        [py] test_progress_answers_api.py
      [py] __init__.py
      [py] achievements.py
      [py] admin.py
      [py] apps.py
      [py] kafka_payloads.py
      [py] kafka_publisher.py
      [py] kafka_results_consumer.py
      [py] models.py
      [py] profile_serializers.py
      [py] serializers.py
      [py] signals.py
      [py] stats.py
      [py] urls.py
      [py] views.py
      [py] viewsets.py
    school_platform/
      admin/
        [py] __init__.py
        [py] views.py
      [py] __init__.py
      [py] asgi.py
      [py] celery.py
      [py] settings.py
      [py] urls.py
      [py] wsgi.py
    subscriptions/
      migrations/
        [py] __init__.py
      [py] __init__.py
      [py] admin.py
      [py] apps.py
      [py] models.py
      [py] tests.py
      [py] views.py
    templates/
      admin/
        auth/
        [html] index.html
    translations/
      migrations/
        [py] 0001_initial.py
        [py] 0002_alter_translationmemory_options_and_more.py
        [py] __init__.py
      signals/
        [py] __init__.py
        [py] specialization.py
        [py] translationmemory.py
      translation_registry/
        [py] __init__.py
        [py] users.py
      [py] __init__.py
      [py] admin.py
      [py] apps.py
      [py] mixins.py
      [py] models.py
      [py] services.py
      [py] tasks.py
    users/
      migrations/
        [py] 0001_initial.py
        [py] 0002_alter_user_bio.py
        [py] 0003_alter_user_options_alter_user_avatar_alter_user_bio_and_more.py
        [py] 0004_specialization_alter_mentor_experience_years_and_more.py
        [py] 0005_specialization_title_en_specialization_title_ru_and_more.py
        [py] 0006_alter_specialization_type.py
        [py] 0007_remove_specialization_translation_status_and_more.py
        [py] 0008_remove_specialization_title_en_and_more.py
        [py] 0009_specialization_description_en_and_more.py
        [py] 0010_mentor_technology.py
        [py] 0011_user_bio_en_user_bio_ru.py
        [py] 0012_remove_user_bio_en_remove_user_bio_ru.py
        [py] 0013_alter_mentor_options_alter_specialization_options_and_more.py
        [py] 0014_user_uuid_public_ids.py
        [py] __init__.py
      tests/
        [py] __init__.py
        [py] conftest.py
        [py] test_manager.py
        [py] test_password_reset.py
        [py] test_profiles.py
        [py] test_user_avatar.py
        [py] test_user_fields.py
        [py] test_user_lifecycle.py
  ... (83 строк дерева скрыто; увеличьте max_lines в скрипте)

## ФРАГМЕНТЫ КЛЮЧЕВЫХ ФАЙЛОВ

  [docker-compose] docker-compose.yml
  (оркестрация сервисов)
  --- начало файла ---
  | services:
  |   db:
  |     image: postgres:15-alpine
  |     container_name: bervinov-academy-db
  |     ports:
  |       - "5433:5432"
  |     env_file: .env
  |     environment:
  |       POSTGRES_DB: ${DB_NAME}
  |       POSTGRES_USER: ${DB_USER}
  |       POSTGRES_PASSWORD: ${DB_PASSWORD}
  |     volumes:
  |       - postgres_data:/var/lib/postgresql/data
  |     healthcheck:
  |       test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
  |       interval: 5s
  |       retries: 5
  |       timeout: 5s
  | 
  |   redis:
  |     image: redis:7-alpine
  |     container_name: bervinov-academy-redis
  |     ports:
  |       - "6379:6379"
  |     volumes:
  |       - redis_data:/data
  |     healthcheck:
  |       test: ["CMD", "redis-cli", "ping"]
  |       interval: 5s
  |       retries: 5
  | 
  |   # Kafka stack: backend/celery in Compose should use KAFKA_BOOTSTRAP_SERVERS=kafka:29092.
  |   # Host tools (kafka-console-consumer) use localhost:9092.
  |   zookeeper:
  |     # Pin: cp-*:latest switched to KRaft-only and breaks ZK env (KAFKA_PROCESS_ROLES).
  |     image: confluentinc/cp-zookeeper:7.6.1
  |     container_name: bervinov-academy-zookeeper
  |     ports:
  |       - "2181:2181"
  |     environment:
  |       ZOOKEEPER_CLIENT_PORT: 2181
  |       ZOOKEEPER_TICK_TIME: 2000
  | 
  |   kafka:
  |     image: confluentinc/cp-kafka:7.6.1
  |     container_name: bervinov-academy-kafka
  |     ports:
  |       - "9092:9092"
  |     depends_on:
  |       - zookeeper
  |     environment:
  |       KAFKA_BROKER_ID: 1
  |       KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
  |       KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
  |       KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:29092,PLAINTEXT_HOST://0.0.0.0:9092
  | ... (193 строк не показано)
  --- конец файла ---

  [nginx] nginx\nginx.conf
  (прокси SPA и API)
  --- начало файла ---
  | events {
  |     worker_connections 1024;
  | }
  | 
  | http {
  |     include       /etc/nginx/mime.types;
  |     default_type  application/octet-stream;
  | 
  |     sendfile        on;
  |     keepalive_timeout  65;
  | 
  |     # Frontend - статические файлы React приложения
  |     server {
  |         listen 80;
  |         server_name localhost;
  | 
  |         # Прокси для API запросов
  |         location /api/ {
  |             proxy_pass http://backend:8000/api/;
  |             proxy_set_header Host $host;
  |             proxy_set_header X-Real-IP $remote_addr;
  |             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  |             proxy_set_header X-Forwarded-Proto $scheme;
  |         }
  | 
  |         # Прокси для админ-панели Django (включая статику)
  |         location /admin/ {
  |             proxy_pass http://backend:8000/admin/;
  |             proxy_set_header Host $host;
  |             proxy_set_header X-Real-IP $remote_addr;
  |             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  |             proxy_set_header X-Forwarded-Proto $scheme;
  |         }
  | 
  |         # Статика Django (CSS, JS, изображения для админки)
  |         location /static/ {
  |             proxy_pass http://backend:8000/static/;
  |             proxy_set_header Host $host;
  |             proxy_set_header X-Real-IP $remote_addr;
  |             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  |             proxy_set_header X-Forwarded-Proto $scheme;
  |         }
  | 
  |         # Медиа файлы (загруженные пользователями)
  |         location /media/ {
  |             proxy_pass http://backend:8000/media/;
  |             proxy_set_header Host $host;
  |             proxy_set_header X-Real-IP $remote_addr;
  |             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  |             proxy_set_header X-Forwarded-Proto $scheme;
  |         }
  | 
  |         # Прокси для Swagger/ReDoc документации API
  |         location /api/schema/ {
  |             proxy_pass http://backend:8000/api/schema/;
  |             proxy_set_header Host $host;
  |             proxy_set_header X-Real-IP $remote_addr;
  |             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  |             proxy_set_header X-Forwarded-Proto $scheme;
  |         }
  | 
  |         location /api/swagger/ {
  |             proxy_pass http://backend:8000/api/swagger/;
  |             proxy_set_header Host $host;
  |             proxy_set_header X-Real-IP $remote_addr;
  |             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  |             proxy_set_header X-Forwarded-Proto $scheme;
  |         }
  | 
  |         location /api/redoc/ {
  |             proxy_pass http://backend:8000/api/redoc/;
  |             proxy_set_header Host $host;
  |             proxy_set_header X-Real-IP $remote_addr;
  |             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  |             proxy_set_header X-Forwarded-Proto $scheme;
  |         }
  | 
  |         # Статические файлы frontend (для production сборки)
  |         location / {
  |             root /usr/share/nginx/html;
  |             try_files $uri $uri/ /index.html;
  |         }
  |     }
  | }
  --- конец файла ---

  [django urls] backend\school_platform\urls.py
  (корневые маршруты Django)
  --- начало файла ---
  | from django.conf import settings
  | from django.conf.urls.static import static
  | from django.contrib import admin
  | from django.http import JsonResponse
  | from django.urls import include, path
  | from drf_spectacular.views import (
  |     SpectacularAPIView,
  |     SpectacularRedocView,
  |     SpectacularSwaggerView,
  | )
  | 
  | 
  | def health_check(request):
  |     """Health check endpoint for Docker."""
  |     return JsonResponse({"status": "ok"})
  | 
  | 
  | def home_view(request):
  |     """Главная страница проекта."""
  |     from django.shortcuts import render
  | 
  |     return render(request, "home.html")
  | 
  | 
  | urlpatterns = [
  |     path("", home_view, name="home"),
  |     path("i18n/", include("django.conf.urls.i18n")),
  |     path("admin/", admin.site.urls),
  |     path("health/", health_check, name="health"),
  |     path("api/", include("users.urls")),
  |     path("api/", include("content.urls")),
  |     path("api/", include("progress.urls")),
  |     path("api/", include("education.urls")),
  |     path("api/", include("mentoring.urls")),
  |     path("api/", include("exams.urls")),
  |     path("api/", include("communication.urls")),
  |     path("api/schema/", SpectacularAPIView.as_view(), name="schema"),
  |     path(
  |         "api/swagger/",
  |         SpectacularSwaggerView.as_view(url_name="schema"),
  |         name="swagger-ui",
  |     ),
  |     path(
  |         "api/redoc/",
  |         SpectacularRedocView.as_view(url_name="schema"),
  |         name="redoc",
  |     ),
  | ]
  | 
  | if settings.DEBUG:
  |     urlpatterns += static(
  |         settings.STATIC_URL, document_root=settings.STATIC_ROOT
  |     )
  |     urlpatterns += static(
  |         settings.MEDIA_URL, document_root=settings.MEDIA_ROOT
  | ... (1 строк не показано)
  --- конец файла ---

  [vite] пропущено — нет файла: C:\Users\Dir\PycharmProjects\BervinovAcademy\frontend\vite.config.js

  [README] README.md
  (человеческое описание проекта)
  --- начало файла ---
  | <div align="center">
  | 
  | # Bervinov Academy
  | 
  | **Образовательная платформа** с курсами, интерактивными заданиями, автопроверкой кода (Kafka + Docker-песочница) и современным SPA-фронтендом.
  | 
  | Python **3.11+** · Django · PostgreSQL · Redis · Celery · Kafka · React
  | 
  | </div>
  | 
  | ---
  | 
  | <!-- ═══════════════════════════════════════════════════════════════════
  |      HERO / АНИМАЦИЯ — вставьте сюда демо
  |      Варианты:
  |      1) GIF: <img src="docs/images/demo.gif" alt="Demo" width="720" />
  |      2) Видео: <video src="docs/images/demo.webm" autoplay muted loop />
  |      3) Lottie: в README.md напрямую Lottie не рендерится — используйте
  |         ссылку на страницу или статичный превью-кадр + GIF ниже.
  |      ═══════════════════════════════════════════════════════════════════ -->
  | 
  | <p align="center">
  |   <a href="#-быстрый-старт-docker">Запуск</a>
  |   &nbsp;·&nbsp;
  |   <a href="#-обновление-проекта">Обновление</a>
  |   &nbsp;·&nbsp;
  |   <a href="#-схема-базы-данных">Схема БД</a>
  |   &nbsp;·&nbsp;
  |   <a href="#-архитектура-сервисов">Архитектура</a>
  |   &nbsp;·&nbsp;
  |   <a href="#-гайд-по-проекту">Гайд</a>
  | </p>
  | 
  | <p align="center">
  |   <!-- TODO: положите файл и раскомментируйте строку ниже -->
  |   <!-- <img src="docs/images/hero-demo.gif" alt="Bervinov Academy demo" width="780" /> -->
  |   <img src="https://via.placeholder.com/780x320/1a1a2e/eeeef7?text=Hero+%2F+Animation+%E2%86%92+docs%2Fimages%2Fhero-demo.gif" alt="Место под анимацию: добавьте docs/images/hero-demo.gif" width="780" />
  | </p>
  | 
  | ---
  | ... (375 строк не показано)
  --- конец файла ---

  [pyproject] pyproject.toml
  (метаданные Python-проекта)
  --- начало файла ---
  | [project]
  | name = "bervinov-academy"
  | version = "0.1.0"
  | description = "Online education platform"
  | requires-python = ">=3.11"
  | 
  | [tool.black]
  | line-length = 79
  | target-version = ['py311']
  | include = '\.pyi?$'
  | extend-exclude = '''
  | /(
  |   | migrations
  |   | .venv
  |   | .git
  |   | __pycache__
  |   | build
  |   | dist
  | )/
  | '''
  | 
  | [tool.isort]
  | profile = "black"
  | multi_line_output = 3
  | line_length = 79
  | known_first_party = [
  |   "backend", "users", "education", "subscriptions",
  |   "progress", "mentoring", "content", "communication",
  |   "translations"
  | ]
  | force_single_line = false
  | force_sort_within_sections = true
  | 
  | [tool.flake8]
  | max-line-length = 79
  | ... (66 строк не показано)
  --- конец файла ---

  [env example] .env.example
  (переменные окружения (шаблон))
  --- начало файла ---
  | # Debug
  | DEBUG=True
  | 
  | # Уровень логов Django: DEBUG, INFO, WARNING, ERROR
  | DJANGO_LOG_LEVEL=INFO
  | 
  | # Secret Key - СГЕНЕРИРУЙ НОВЫЙ ДЛЯ ПРОДАКШЕНА!
  | SECRET_KEY=your-super-secret-key-here-change-in-production
  | 
  | # Database PostgreSQL
  | DB_ENGINE=django.db.backends.postgresql
  | DB_NAME=your_database_name_here
  | DB_USER=your_database_user_here
  | DB_PASSWORD=your-strong-database-password-here
  | DB_HOST=localhost
  | DB_PORT=5432
  | 
  | # Django Security
  | ALLOWED_HOSTS=localhost,127.0.0.1,.localhost
  | CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000
  | 
  | # Frontend
  | FRONTEND_URL=http://localhost:3000
  | 
  | # Kafka (kafka-python; new CodeSubmission is published to this topic)
  | # Django on host: localhost:9092. Backend in docker-compose: kafka:29092
  | KAFKA_BOOTSTRAP_SERVERS=localhost:9092
  | KAFKA_TOPIC=code-submissions
  | # Результаты проверки: воркер code-check-runner пишет сюда;
  | # ``python manage.py consume_code_submission_results``
  | # (сервис kafka-results-consumer в docker-compose) читает и обновляет CodeSubmission.
  | KAFKA_TOPIC_CODE_RESULTS=code-submission-results
  | # Отдельная consumer group для Django (не совпадать с group воркера проверки).
  | KAFKA_GROUP_CODE_RESULTS=django-code-submission-results
  | KAFKA_RESULTS_AUTO_OFFSET_RESET=earliest
  | ... (11 строк не показано)
  --- конец файла ---

## DOCKER COMPOSE (полный текст, если уместен)

  services:
    db:
      image: postgres:15-alpine
      container_name: bervinov-academy-db
      ports:
        - "5433:5432"
      env_file: .env
      environment:
        POSTGRES_DB: ${DB_NAME}
        POSTGRES_USER: ${DB_USER}
        POSTGRES_PASSWORD: ${DB_PASSWORD}
      volumes:
        - postgres_data:/var/lib/postgresql/data
      healthcheck:
        test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
        interval: 5s
        retries: 5
        timeout: 5s
  
    redis:
      image: redis:7-alpine
      container_name: bervinov-academy-redis
      ports:
        - "6379:6379"
      volumes:
        - redis_data:/data
      healthcheck:
        test: ["CMD", "redis-cli", "ping"]
        interval: 5s
        retries: 5
  
    # Kafka stack: backend/celery in Compose should use KAFKA_BOOTSTRAP_SERVERS=kafka:29092.
    # Host tools (kafka-console-consumer) use localhost:9092.
    zookeeper:
      # Pin: cp-*:latest switched to KRaft-only and breaks ZK env (KAFKA_PROCESS_ROLES).
      image: confluentinc/cp-zookeeper:7.6.1
      container_name: bervinov-academy-zookeeper
      ports:
        - "2181:2181"
      environment:
        ZOOKEEPER_CLIENT_PORT: 2181
        ZOOKEEPER_TICK_TIME: 2000
  
    kafka:
      image: confluentinc/cp-kafka:7.6.1
      container_name: bervinov-academy-kafka
      ports:
        - "9092:9092"
      depends_on:
        - zookeeper
      environment:
        KAFKA_BROKER_ID: 1
        KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
        KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
        KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:29092,PLAINTEXT_HOST://0.0.0.0:9092
        KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
        KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
        KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
        KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
        KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      healthcheck:
        test:
          [
            "CMD",
            "kafka-broker-api-versions",
            "--bootstrap-server",
            "localhost:9092",
          ]
        interval: 10s
        timeout: 10s
        retries: 12
        start_period: 40s
  
    backend:
      build:
        context: .
        dockerfile: backend/Dockerfile
      container_name: bervinov-academy-backend
      command: >
        sh -c "cd /app/backend &&
               python manage.py compilemessages -l en &&
               python manage.py migrate &&
               python manage.py runserver 0.0.0.0:8000"
      volumes:
        - .:/app
      ports:
        - "8000:8000"
      env_file: .env
      depends_on:
        db:
          condition: service_healthy
        redis:
          condition: service_healthy
        kafka:
          condition: service_healthy
      working_dir: /app/backend
      healthcheck:  # Добавлен healthcheck для backend
        test: ["CMD", "curl", "-f", "http://localhost:8000/health/"]
        interval: 10s
        timeout: 5s
        retries: 5
        start_period: 30s
  
    frontend:
      image: alpine:3.20
      container_name: bervinov-academy-frontend
      volumes:
        - ./frontend:/app
      working_dir: /app
      command: >
        sh -c "rm -rf dist && mkdir -p dist &&
               cp index.html dist/ &&
               cp -r src dist/ &&
               (test -d public && cp -r public dist/ || true)"
      restart: "no"
  
    nginx:
      build:
        context: ./nginx
        dockerfile: Dockerfile
      container_name: bervinov-academy-nginx
      ports:
        - "80:80"
      volumes:
        - ./frontend/dist:/usr/share/nginx/html:ro
      depends_on:
        frontend:
          condition: service_completed_successfully
        backend:
          condition: service_healthy
      restart: unless-stopped
  
    celery:
      build:
        context: .
        dockerfile: backend/Dockerfile
      container_name: bervinov-academy-celery
      command: >
        sh -c "cd /app/backend &&
               python manage.py compilemessages -l en &&
               celery -A school_platform worker --loglevel=info"
      volumes:
        - .:/app
      env_file: .env
      depends_on:
        db:
          condition: service_healthy
        redis:
          condition: service_healthy
        kafka:
          condition: service_healthy
      working_dir: /app/backend
      healthcheck:
        disable: true
  
    celery-beat:
      build:
        context: .
        dockerfile: backend/Dockerfile
      container_name: bervinov-academy-celery-beat
      command: >
        sh -c "cd /app/backend &&
               python manage.py compilemessages -l en &&
               celery -A school_platform beat --loglevel=info"
      volumes:
        - .:/app
      env_file: .env
      depends_on:
        db:
          condition: service_healthy
        redis:
          condition: service_healthy
      working_dir: /app/backend
      healthcheck:
        disable: true
  
    # Образ песочницы (только build; entrypoint заменён, чтобы сервис сразу
    # завершился — runner использует image при docker run).
    code-check-sandbox:
      image: bervinov-academy-code-sandbox:local
      build:
        context: ./services/code_check_sandbox
      entrypoint: ["/bin/true"]
      restart: "no"
  
    code-check-runner:
      build:
        context: ./services/code_check_emulator
      image: bervinov-academy-code-check-runner:local
      container_name: bervinov-academy-code-check-runner
      restart: unless-stopped
      environment:
        KAFKA_BOOTSTRAP_SERVERS: kafka:29092
        KAFKA_TOPIC_IN: code-submissions
        KAFKA_TOPIC_CODE_RESULTS: code-submission-results
        KAFKA_GROUP_ID: code-check-runner
        KAFKA_AUTO_OFFSET_RESET: earliest
        SANDBOX_IMAGE: bervinov-academy-code-sandbox:local
        MAX_CONCURRENT_RUNS: "2"
        DOCKER_RUN_TIMEOUT_BUFFER_SEC: "5"
  ... (48 строк не показано)

================================================================================
КОНЕЦ ФАЙЛА КОНТЕКСТА
================================================================================
